import { lazy } from 'react'

const {{pascalCase (combinedName group name layer)}} = lazy(() => import('./ui/{{kebabCase (combinedName group name layer)}}'))
export { {{pascalCase (combinedName group name layer)}} }
