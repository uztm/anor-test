import cn from './{{kebabCase (combinedName group name layer)}}.module.css'

interface I{{pascalCase (combinedName group name layer)}}Props {}

export default function {{pascalCase (combinedName group name layer)}}({}: Readonly<I{{pascalCase (combinedName group name layer)}}Props>) {
  return (
    <div>
      {{pascalCase (combinedName group name layer)}} component
    </div>
  )
}
